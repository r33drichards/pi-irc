{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "mcp-js/Dockerfile", "watchPatterns": ["mcp-js/**"] },
  "deploy": { "restartPolicyType": "ALWAYS", "numReplicas": 1 }
}
